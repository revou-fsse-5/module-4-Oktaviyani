import React from 'react'

function Event() {
  return (
    <div>
        <h1>Event</h1>
        <button onClick={() => alert('test click')}

        >Click</button>
    </div>
  )
}

export default Event